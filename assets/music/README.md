# Background Music Pool

Drop your background audio tracks in `.mp3` or `.wav` format here.

The engine will automatically select background music from this pool and apply soft audio ducking (-18dB to -22dB) under the main TTS voiceover track.
